# wrangler.toml 파일 내용 (예시)
name = "youth-career-studio-final-v4"
type = "javascript"

# 다음 라인을 추가하거나 수정
pages_build_output_dir = ".next" # 또는 빌드 결과물이 저장되는 최종 경로
# 또는
# pages_build_output_dir = "out" # Next.js의 static export를 사용하는 경우

compatibility_date = "2025-11-16" # 최신 날짜로 설정
